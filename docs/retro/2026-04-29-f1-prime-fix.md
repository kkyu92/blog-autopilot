# F1' Reliability Fix 박제 + 5/6 평가 baseline (2026-04-29)

**작성 시점**: 2026-04-29 14:50 KST
**선행 retro**: `docs/retro/2026-04-28-mid-review.md` (4/28 점검 결과 v0)
**선행 spec**: `docs/superpowers/specs/2026-04-29-cloudflare-cron-design.md` (Cloudflare Phase 1 — 진입 trigger 미충족 시 보류)
**관련 commit**: `0331737` (F1' 5종 fix), `6914997` (test 반영)

---

## TL;DR

**4/29 발행 fail (8/9 success → 12건 누적)** = ✅ **F1' 5종 fix 효과 확인**
**가설 H1·H2 (fd 고갈 + orphan accumulation)** = ✅ **강하게 지지** — spawn-related fail 0건
**5/6 평가 윈도우** = ⏳ **4/30~5/6 자연 cron 7회 + manual 데이터로 root cause 확정**

→ **F1' 만으로 80%+ 회복 시 F2' (Anthropic API 마이그레이션) 폐기**, $0/mo VISION.md 조건 유지.

---

## 1. 발견 — 4/29 첫 dispatch fail evidence

### 1.1 dispatch `25085433470` (4/29 09:52 KST 시작, 11:47 KST 종료)

| 항목 | 값 |
|---|---|
| conclusion | **failure** |
| duration | 115분 (115min, 직렬 처리) |
| success / fail | 4 / 5 |
| discard ratio | **56%** ≥ 50% threshold → exit 1 |
| healthcheck | PASS ✅ (A1 fix `724ca90` 작동 확인) |

### 1.2 5 fail의 sub-cause (gh run logs evidence)

| # | niche / keyword | 직접 원인 | 코드 위치 |
|---|---|---|---|
| 1 | WS / 알츠하이머 | `writer: writer: missing field title` (이중 prefix) | `writer.ts:246` throw + `auto-publish.ts:444` catch |
| 2 | WS / 대사증후군 | `claude CLI timeout after 600000ms` (10분 hang → SIGTERM) | `llm.ts:103` |
| 3 | TS / 충남 봄꽃 | `writer: writer: missing field title` | 동일 (#1) |
| 4 | TS / 5월 전남 섬 | `spawn claude ENOENT` | `llm.ts:80` (claude 바이너리 못 찾음) |
| 5 | AS / 청약 부양가족 | `spawn Unknown system error -8` | `llm.ts:80` (EFAULT/EIO) |

### 1.3 시간 패턴 (slot 분포)

| niche | slot 1 | slot 2 | slot 3 |
|---|---|---|---|
| WS (10:17~10:40 KST) | ✅ 발행 | ❌ fail | ❌ fail |
| TS (10:48~11:00 KST) | ✅ 발행 | ❌ fail | ❌ fail |
| AS (11:06~11:47 KST) | ✅ 발행 | ✅ 발행 | ❌ fail |

→ **첫 슬롯은 모두 success, 후행 슬롯에서 fail 집중**. claude CLI 누적 spawn 후 OS-level 불안정.

---

## 2. 가설 매트릭스 (root cause 후보)

| # | 가설 | F1'으로 fix? | F2'(API)으로 fix? | 검증법 |
|---|---|---|---|---|
| **H1** | fd limit / OS file descriptor 고갈 | ✅ ulimit 증가 | ✅ HTTP fd 1개 | runner `ulimit -n` 확인 — **256 (macOS 기본) 확정** |
| **H2** | orphan claude.exe 누적 → spawn 충돌 | ✅ 명시적 kill | ✅ spawn 안 함 | 4/28 post-cleanup logs evidence — **orphan 3개 확인** |
| **H3** | Claude Max 5시간당 메시지 한도 | ❌ 한도 그대로 | ✅ 별도 API quota | 호출 카운트 + uptime instrumentation 필요 |
| **H4** | 인증 token 만료 / session rotation | ❌ 동일 | ✅ API key 안 만료 | 첫 fail 시각 vs healthcheck PASS 시각 비교 |
| **H5** | claude CLI 자체 버그 | ❌ Anthropic 측 fix만 | ✅ CLI 안 씀 | 외부 의존, 검증 어려움 |
| **H6** | self-hosted runner home-mbp memory pressure | ✅ memory monitor | ✅ HTTP는 적은 메모리 | Activity Monitor — 32GB RAM, 메모리 여유 충분 (가능성 낮음) |

→ **H1·H2 가장 가능성 높음** (ulimit 256 확정 + orphan evidence). F1' 5종 fix 우선 적용 후 검증.

---

## 3. F1' 5종 sub-fix (commit `0331737`)

### 3.1 변경 매트릭스

| Sub-fix | 파일 | 변경 | 대응 가설 |
|---|---|---|---|
| **F1'-e** | `.github/workflows/auto-publish.yml` | `ulimit -n 10240` (256 → 10240, 40배) | H1 |
| **F1'-c** | `src/lib/llm.ts` | spawn ENOENT/EFAULT/-8 5초 후 1회 retry | transient OS-level |
| **F1'-a** | `src/lib/llm.ts` | 호출 카운트 + uptime instrumentation (`getClaudeCallStats`) | H3·H4 검증 데이터 |
| **F1'-b** | `scripts/auto-publish.ts` | cron 시작 직전 `pkill -9 -f "claude -p"` | H2 |
| **F1'-d** | `scripts/auto-publish.ts` | niche간 `Promise.all` 병렬 (직렬 → niche별 동시) | H4 시간 의존 + 전체 시간 단축 |

### 3.2 안전 장치

- **F1'-b orphan kill**: `claude -p` 패턴만 매칭 → 사용자 Claude Code 인터랙티브 session (다른 인자) 영향 X
- **F1'-c retry**: timeout(`timeout after`)은 제외 — 10분 hang은 transient 아님, escalate
- **F1'-d 병렬**: niche간만 동시 (최대 3 spawn). niche 안 slot 1→2→3는 직렬 유지. 메모리 ~3GB 추정, 32GB RAM 안전

### 3.3 검증 로컬

- `pnpm test src/lib/__tests__/llm.test.ts` → **21/21 pass** (commit `6914997`에서 retry 동작 반영 testTimeout 5s → 15s)

---

## 4. 검증 — 4/29 두 번째 dispatch (`25091647631`)

### 4.1 결과

| 항목 | 첫 dispatch (직렬) | 두 번째 dispatch (F1' + 병렬) | 변화 |
|---|---|---|---|
| conclusion | **failure** | **success** ✅ | 정상화 |
| duration | 115분 | **47분** | **-60%** |
| success / fail | 4 / 5 | **8 / 1** | **44% → 89%** |
| exit code | 1 (discard 56%) | 0 | clean |
| spawn-related fail | 3건 (timeout, ENOENT, spawn -8) | **0건** | F1' 효과 확정 |
| writer missing title | 2건 | 0건 | 영향 없음 |

### 4.2 instrumentation logs evidence (F1'-a)

```
[ulimit] file descriptors: 10240
[orphan-kill] pre-cron sweep complete (claude -p 패턴)
[auto-publish] niche간 병렬 시작: WS,TS,AS (각 3 slot)
[llm] call #1 (uptime 0.0min) model=sonnet expectJson=false
... (호출 누적 31회)
[llm-stats] claude calls=31, uptime=46.1min
[auto-publish] success: 8, failed: 1, skipped: 0
```

→ **31 calls / 46.1 min = 평균 1.5min/call**. 직렬 (115min, 36 calls = 3.2min/call) 대비 **호출 간 wall-clock 절반 이하**. niche간 동시 spawn 작동 확정.

### 4.3 4/29 누적 발행 (DB `published_posts`)

| niche | platform | 4/29 첫 dispatch | 4/29 두 번째 dispatch | **합계** |
|---|---|---|---|---|
| WS | wordpress_ws | 1 | 2 | **3** ✅ |
| TS | wordpress_ts | 1 | 3 | **4** (+1 oversupply) |
| AS | blogger_as | 2 | 3 | **5** (+2 oversupply) |
| **합계** | — | **4** | **8** | **12** |

→ 사용자 의도 (각 블로그 ≥ 3건) **충족 + oversupply**. 4/29 발행은 회복 완료.

### 4.4 남은 1 fail

```
WS / 5월 가정의 달 생애주기별 가족 건강 체크리스트
→ writer: editor: missing status/verdict (raw keys: final_meta)
```

**spawn 문제 아님** — editor LLM이 `status`·`verdict` 필드 없이 `final_meta`만 반환. JSON drift, F1' 영역 밖. 별도 의제 (editor 코드 fallback 추가 또는 재시도).

---

## 5. 5/6 평가 plan

### 5.1 측정 윈도우

| 시작 | 끝 | 의미 |
|---|---|---|
| 2026-04-30 KST 01:17 | 2026-05-06 KST 03:00 | 7회 자연 schedule cron + 추가 manual dispatch |

### 5.2 측정 지표

```bash
# cron success rate (window 4/29~5/5)
gh run list --workflow=auto-publish.yml --limit 30 --json conclusion | \
  jq 'group_by(.conclusion) | map({c: .[0].conclusion, n: length})'

# 일별 발행 cover율
sqlite3 data/blog.db "SELECT date(published_at,'localtime'), niche, COUNT(*) \
  FROM published_posts WHERE date(published_at,'localtime') >= '2026-04-30' \
  GROUP BY 1, 2 ORDER BY 1, 2"
```

### 5.3 분기점

| 5/6 success rate | root cause 시사 | 다음 액션 |
|---|---|---|
| ≥ 90% | **H1·H2 거의 전부 cover** | F1' 종결, F2' 폐기, $0 유지. retro update |
| 80~89% | H1·H2 + 일부 H3·H5 잔존 (예: 4/29 두 번째 dispatch와 비슷) | F1' 충분, editor missing status/verdict 별도 fix |
| 60~79% | H3·H4 또는 H5 잔존 | instrumentation logs로 root cause 분석, F2' 부분 도입 검토 |
| < 60% | F1'으로 cover 못 함 | F2' (Anthropic API HTTP 직접 호출) escalate, +$66/mo |

### 5.4 instrumentation 활용

각 cron logs의 `[llm-stats] claude calls=N, uptime=Mmin` 값으로:
- **호출 카운트 의존 fail**: fail 시점의 call # 일정한가? → H1·H2 잔존
- **시간 의존 fail**: fail 시점의 uptime 일정한가? → H4 token expiration

---

## 6. 별도 의제 (이 retro 미포함)

- **editor missing status/verdict fallback** — 4/29 두 번째 dispatch의 1 fail 원인. editor LLM JSON drift 재시도 또는 fallback 처리 추가
- **discard ratio 50% threshold 재평가** — 9 slot 환경에서 5건 fail 시 자동 exit 1. F1' 후 success rate 향상되면 threshold 그대로 유지 OK
- **Cloudflare Phase 1 진입 trigger 재평가** — 5/6 success rate 결과로 Cloudflare 도입 결정 (spec `2026-04-29-cloudflare-cron-design.md` §1.3)

---

## 7. 검증 명령어 (재사용)

### 7.1 F1' 코드 변경 위치

```bash
git show 0331737 --stat
# → 3 files changed: auto-publish.yml +7, auto-publish.ts +88-29, llm.ts +48-1
```

### 7.2 dispatch logs (F1' instrumentation)

```bash
RUN_ID=25091647631  # 4/29 두 번째 dispatch
gh run view $RUN_ID --log | grep -E "ulimit|orphan-kill|niche간 병렬|llm-stats|llm\] call|spawn-level transient"
```

### 7.3 4/30~5/6 일별 cron 결과

```bash
gh run list --workflow=auto-publish.yml --event=schedule --limit 7 --json databaseId,conclusion,createdAt
```

---

## 8. 결정 기록

- **2026-04-29 13:55 KST**: F1' 5종 sub-fix commit `0331737` + push origin main. niche간 병렬 + ulimit 10240 + orphan kill + spawn retry + instrumentation
- **2026-04-29 13:55~14:43 KST**: dispatch `25091647631` — 47분 운영, 8/9 success
- **2026-04-29 14:50 KST**: 본 retro 박제 (5/6 평가 baseline)
- **2026-05-06 KST 03:00**: success rate 측정 + F2' escalate 결정 시점 (별도 세션)

---

## 변경 이력

- **2026-04-29 (작성)**: 4/29 첫 dispatch fail evidence + F1' 5종 sub-fix + 두 번째 dispatch 검증 (8/9 success) + 5/6 평가 frame 박제. H1·H2 가설 강하게 지지.
