# Fail pnpm audit when vulnerabilities at or above this level are found.
# critical: RCE / full compromise (blocks CI)
# high: significant exploit potential (tracked via security-audit.yml)
audit-level=critical
